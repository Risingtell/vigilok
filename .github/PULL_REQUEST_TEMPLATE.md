## What changed and why

## How was this verified?
- [ ] `npm run build` passes
- [ ] `npm run smoke` passes (no credentials needed)
- [ ] If this touches a paid route or the Aave math: tested against a real deployment/real position, not just typechecked

## Anything a reviewer should look at closely?
